[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lifekernel"
version = "0.1.0"
description = "LifeKernel project"
requires-python = ">=3.10"
dependencies = [
    "typing>=3.10.0.0",
]

[tool.uv]
package = false

